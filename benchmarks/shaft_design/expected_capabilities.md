# Expected capabilities — shaft_design

The lab should:

- Route to **STANDARD** (SIMPLE is FAIL; COMPLEX may be PARTIAL).
- Decompose: torque → material assumption → strength criterion → diameter.
- Keep material grade as an **explicit assumption**, not FACT.
- Run deterministic calculation + unit checks.
- Include independent **verification** node.
- Detect wrong units/formulas via checks when present on claims.
